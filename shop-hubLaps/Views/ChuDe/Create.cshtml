@model shop_hubLaps.Models.ChuDe

@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    ViewData["Title"] = "Thêm Chủ Đề";
}

<h1 class="text-start" style="font-family: Lexend; font-weight: 500; font-size: 30px;">@ViewData["Title"]</h1>

<form asp-action="Create" method="post" enctype="multipart/form-data">
    <div class="row">
        <div class="col-md-6 mb-3">
            <div class="form-group">
                <label asp-for="tenchude" class="control-label">Tên Chủ Đề</label>
                <input asp-for="tenchude" class="form-control" />
                <span asp-validation-for="tenchude" class="text-danger"></span>
            </div>
        </div>

        <div class="col-md-6 mb-3">
            <div class="form-group">
                <label asp-for="slug" class="control-label">Slug</label>
                <input asp-for="slug" class="form-control" />
                <span asp-validation-for="slug" class="text-danger"></span>
            </div>
        </div>

        <div class="col-md-6 mb-3">
            <div class="form-group">
                <label asp-for="hinh" class="control-label">Hình ảnh</label>
                <input type="file" name="hinh" class="form-control" />
                <span asp-validation-for="hinh" class="text-danger"></span>
            </div>
        </div>

    </div>

    <button type="submit" class="btn btn-primary">Lưu</button>
    <a asp-action="Index" class="btn btn-secondary ml-2">Quay về</a>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
