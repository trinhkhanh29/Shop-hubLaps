@model shop_hubLaps.Models.MetaLaptop

@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    ViewData["Title"] = "Thêm Meta Laptop";
}

<h1>Thêm Meta Laptop</h1>

<form method="post">
    <div class="form-group">
        <label for="keymeta">Key Meta</label>
        <input type="text" class="form-control" id="keymeta" name="keymeta" value="@Model?.keymeta" required />
    </div>

    <div class="form-group">
        <label for="valuemeta">Value Meta</label>
        <input type="text" class="form-control" id="valuemeta" name="valuemeta" value="@Model?.valuemeta" required />
    </div>

@*     <div class="form-group">
        <label for="LaptopId">Laptop</label>
        <select class="form-control" id="LaptopId" name="malaptop">
            <option value="">-- Select Laptop --</option>
            @foreach (var laptop in ViewData["LaptopList"] as SelectList)
            {
                <option value="@laptop.Value" selected="@(laptop.Value == Model?.malaptop.ToString() ? "selected" : "")">
                    @laptop.Text (@laptop.Hang.tenhang) <!-- Display laptop name and brand -->
                </option>
            }
        </select>
    </div> *@

    <button type="submit" class="btn btn-primary">Create</button>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
