@model shop_hubLaps.Models.TinTuc

<div class="container">
    <h1>@Model.tieude</h1>
    <p class="text-muted">Chủ đề: @Model.ChuDe.tenchude | Ngày cập nhật: @Model.ngaycapnhat?.ToString("dd/MM/yyyy")</p>
    <img src="@Model.hinhnen" alt="@Model.tieude" style="width: 100%; max-height: 400px; object-fit: cover; margin-bottom: 20px;" />
    <p>@Model.noidung</p>
    <p><strong>Lượt xem:</strong> @Model.luotxem</p>
    <a href="@Url.Action("Index", "Blog")" class="btn btn-secondary">Quay lại</a>
</div>
